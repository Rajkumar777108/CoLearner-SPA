import { ColorHighlighterDirective } from './color-highlighter.directive';

describe('ColorHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
